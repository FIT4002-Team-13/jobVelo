.venv/
venv*/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

.env
!.env.example

# User uploads (company logos, CVs, etc.) - never committed.
# Keep the directory itself so a fresh clone has somewhere to write to.
uploads/*
!uploads/.gitkeep

# Live-highlight debug dump (US18) - disposable, local-only inspection of
# what the AI is marking as important during a live interview.
highlighted_transcript_testing.json
